[project]
name = "growth-monthly-report"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "clickhouse-connect>=0.8",
    "pandas>=2.2",
    "python-dotenv>=1.0",
]

[tool.uv]
dev-dependencies = [
    "ipykernel>=7.2.0",
]
